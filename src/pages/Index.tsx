import { ProposalDashboard } from "@/components/ProposalDashboard";
import { mockProposalData } from "@/data/mockProposalData";

const Index = () => {
  return <ProposalDashboard proposalData={mockProposalData} />;
};

export default Index;
