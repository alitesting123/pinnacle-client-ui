version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        # ✅ CloudFront URL for production
        - echo "VITE_API_BASE_URL=https://dlndpgwc2naup.cloudfront.net" >> .env.production
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
